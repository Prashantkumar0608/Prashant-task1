# Prashant_task1

A simple HTML project containing a basic `index.html` page.

## Overview

- `index.html` displays a simple "hello world" page.

## Usage

1. Open `index.html` in a web browser.
2. The page will render a heading with the text `hello world`.

## Notes

- This project currently contains only a single static HTML file.
